import styled from '@xstyled/styled-components'

const Header = styled.div`
  text-align: center;
  margin: 15px 0;

  h1 {
  	color: textColor;
  }

  .card-body {

  	p {
  		margin: 0;
  	}
  }
`
export default Header