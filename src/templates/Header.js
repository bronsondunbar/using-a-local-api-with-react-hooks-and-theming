import styled from '@xstyled/styled-components'

const Header = styled.div`
  text-align: center;
  margin: 15px 0;

  h1 {
  	color: textColor;

    a {
      color: textColor;      
    }
  }

  .card {
    .card-body {
      p {
        margin: 0;
        text-align: center;
      }
    }
  }
`
export default Header